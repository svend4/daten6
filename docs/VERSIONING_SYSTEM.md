# Система Многомерного Версионирования и Автоматического Преобразования Контента

## 📐 Концепция: Два Вектора Развития

### Основная Идея

Контент развивается **одновременно** по двум осям:

1. **Вертикальная ось (Глубина)** - детализация описания
2. **Горизонтальная ось (Время)** - версии изменений

```
                    Упрощение ↑
                              |
                    Заголовок (2 слова)
                              |
         v1 ←————————————————— ○ ————————————————→ v2
                              |
                    Краткое описание (2 предложения)
                              |
                    Расширенное (6 предложений)
                              |
                    Детальное (20+ предложений)
                              |
                    Усложнение ↓
```

---

## 🎯 Ключевые Принципы

### Принцип 1: **Ничего Не Удаляется**

❌ **НИКОГДА не удалять информацию**
✅ **Информация переходит в заголовки или архивные версии**

```
История развития контента:

Версия 1.0:
  Заголовок: "Меню"
  Описание: "Наше меню включает супы и вторые блюда."

↓ При появлении детального описания ↓

Версия 2.0:
  Заголовок: "Меню"
  Под-заголовок: "Наше меню включает супы и вторые блюда."
  Описание: "Мы предлагаем большой выбор супов: борщ, солянка,
             уха. Вторые блюда представлены стейками, пастой,
             рыбными деликатесами. Все блюда готовятся из
             свежих продуктов."

↓ При дальнейшем расширении ↓

Версия 3.0:
  Заголовок: "Меню"
  Под-заголовок-1: "Наше меню включает супы и вторые блюда."
  Под-заголовок-2: "Мы предлагаем большой выбор супов..."
  Детальное описание: [20+ предложений с историей, рецептами,
                       составом, ценами, отзывами...]
```

### Принцип 2: **Автоматическое Преобразование**

Контент **автоматически перемещается** вверх по иерархии при появлении более детальной информации:

```
Уровень детализации:

L0: Заголовок (2-3 слова)              ← Самый верхний уровень
L1: Краткий заголовок (2 предложения)  ← Стал заголовком из описания
L2: Описание (6 предложений)           ← Стало заголовком из детального
L3: Детальное (20 предложений)         ← Текущее описание
L4: Полное (50+ предложений)           ← Появилось новое, детальное
L5: Исчерпывающее (100+ предложений)   ← И так далее...
```

### Принцип 3: **Конвейер Заголовков**

Как на **производственном конвейере**, контент движется вверх:

```
[Новое детальное описание] →
    ↓
[Текущее описание становится заголовком] →
    ↓
[Предыдущий заголовок становится над-заголовком] →
    ↓
[Главный заголовок остается неизменным]
```

---

## 🔢 Система Индексации

### Многомерная Система Кодирования

Каждый элемент имеет **несколько кодов** одновременно:

```javascript
{
  // 1. КОД ВЕРСИИ (горизонтальная ось)
  version: "v2.3",

  // 2. КОД МОДУЛЯ (категория/тип)
  moduleCode: "MOD-MENU-001",

  // 3. КОД ИЕРАРХИИ (вертикальная ось - глубина)
  hierarchyPath: "1.2.3.4",

  // 4. КОД УРОВНЯ ДЕТАЛИЗАЦИИ
  detailLevel: "L3",

  // 5. КОД КОММЕНТАРИЯ/РЕВИЗИИ
  revisionCode: "REV-2024-12-28-001"
}
```

### 1. Версии (Горизонтальная Ось)

**Формат:** `v{major}.{minor}.{patch}`

```
v1.0 - Первоначальная версия
v1.1 - Минорное обновление (добавлено описание)
v1.2 - Минорное обновление (расширено описание)
v2.0 - Мажорное обновление (структурные изменения)
v2.1 - Минорное обновление
```

**Критерии изменения версии:**

- **Patch (v1.0.1)** - исправление опечаток, мелкие правки
- **Minor (v1.1.0)** - добавление 1-2 уровней детализации
- **Major (v2.0.0)** - добавление 3+ уровней или структурные изменения

### 2. Модули (Категорирование)

**Формат:** `{PREFIX}-{CATEGORY}-{NUMBER}`

```
MOD-MENU-001    - Модуль меню #001
MOD-STAMPS-015  - Модуль марок #015
MOD-WIDGET-042  - Виджет #042
MOD-PAGE-008    - Страница #008
```

**Префиксы:**
- `MOD` - Модуль (основной контент)
- `WGT` - Виджет (интерактивный элемент)
- `PG` - Страница (статическая страница)
- `API` - API интеграция

### 3. Иерархия (Вертикальная Ось - Глубина Вложенности)

**Формат:** `{level1}.{level2}.{level3}...`

```
1           - Корневой элемент
1.1         - Первый подэлемент
1.1.1       - Подэлемент второго уровня
1.1.1.1     - Подэлемент третьего уровня
1.1.1.1.1   - И так до бесконечности...
```

### 4. Уровень Детализации (Вертикальная Ось - Детальность)

**Формат:** `L{number}`

```
L0 - Заголовок (2-3 слова)
L1 - Краткий (1-2 предложения, ~50 символов)
L2 - Средний (3-6 предложений, ~200 символов)
L3 - Расширенный (7-20 предложений, ~500 символов)
L4 - Детальный (21-50 предложений, ~1500 символов)
L5 - Полный (51-100 предложений, ~3000 символов)
L6+ - Исчерпывающий (100+ предложений, книга)
```

### 5. Ревизии/Комментарии

**Формат:** `REV-{YYYY-MM-DD}-{NUMBER}` или `COM-{NUMBER}`

```
REV-2024-12-28-001  - Первая ревизия 28 декабря 2024
REV-2024-12-28-002  - Вторая ревизия
COM-001             - Комментарий #001
COM-002             - Комментарий #002
```

---

## 📊 Структура Данных

### Расширенная Mongoose Schema

```javascript
const ContentVersionSchema = new mongoose.Schema({
  // ========================================
  // ОСНОВНАЯ ИНФОРМАЦИЯ
  // ========================================

  _id: ObjectId,

  // Оригинальный ID (не меняется при версионировании)
  originalId: {
    type: ObjectId,
    required: true,
    index: true
  },

  // ========================================
  // СИСТЕМА ВЕРСИОНИРОВАНИЯ
  // ========================================

  // 1. Версия (горизонтальная ось)
  version: {
    major: { type: Number, default: 1 },
    minor: { type: Number, default: 0 },
    patch: { type: Number, default: 0 },
    full: { type: String, default: 'v1.0.0' }  // "v1.2.3"
  },

  // 2. Модуль
  moduleCode: {
    type: String,
    required: true,
    index: true
    // Формат: "MOD-MENU-001"
  },

  // 3. Иерархия (вложенность)
  hierarchy: {
    path: {
      type: String,
      required: true,
      index: true
      // Формат: "1.2.3.4"
    },
    level: {
      type: Number,
      required: true
      // 0, 1, 2, 3, ...
    },
    parentPath: String  // "1.2.3"
  },

  // 4. Уровень детализации
  detailLevel: {
    code: {
      type: String,
      enum: ['L0', 'L1', 'L2', 'L3', 'L4', 'L5', 'L6+'],
      default: 'L1'
    },
    characterCount: Number,
    sentenceCount: Number,
    wordCount: Number
  },

  // 5. Ревизия
  revision: {
    code: String,         // "REV-2024-12-28-001"
    number: Number,       // Порядковый номер ревизии
    timestamp: Date
  },

  // ========================================
  // КОНТЕНТ (Многоуровневый)
  // ========================================

  content: {
    // Уровень 0: Главный заголовок (неизменяемый)
    mainTitle: {
      type: String,
      required: true,
      maxlength: 50
    },

    // Уровень 1-6+: Иерархия заголовков
    headers: [{
      level: Number,      // 1, 2, 3, ...
      text: String,
      detailLevel: String // L1, L2, L3...
    }],

    // Текущее основное описание
    description: {
      type: String,
      required: true
    },

    // История описаний (архив)
    descriptionHistory: [{
      text: String,
      detailLevel: String,
      timestamp: Date,
      becameHeaderAtVersion: String  // "v2.0"
    }]
  },

  // ========================================
  // СВЯЗИ И ТРАНСФОРМАЦИИ
  // ========================================

  // Предыдущая версия
  previousVersion: {
    type: ObjectId,
    ref: 'ContentVersion'
  },

  // Следующая версия
  nextVersion: {
    type: ObjectId,
    ref: 'ContentVersion'
  },

  // Родительский элемент в иерархии
  parentId: {
    type: ObjectId,
    ref: 'ContentVersion'
  },

  // Дочерние элементы
  children: [{
    type: ObjectId,
    ref: 'ContentVersion'
  }],

  // История трансформаций
  transformations: [{
    type: {
      type: String,
      enum: ['expansion', 'promotion_to_header', 'version_update', 'split', 'merge']
    },
    fromLevel: String,      // "L2"
    toLevel: String,        // "L3"
    timestamp: Date,
    triggeredBy: String,    // Причина трансформации
    automatedBy: String     // Автоматическое правило
  }],

  // ========================================
  // МЕТАДАННЫЕ
  // ========================================

  metadata: {
    author: String,
    source: String,
    language: String,
    tags: [String],
    customFields: Map
  },

  // Флаги состояния
  status: {
    isCurrent: Boolean,      // Текущая активная версия
    isArchived: Boolean,     // Архивная версия
    isPromoted: Boolean      // Стала заголовком
  },

  timestamps: {
    createdAt: Date,
    updatedAt: Date,
    promotedAt: Date,       // Когда стала заголовком
    archivedAt: Date
  }
});

// ========================================
// ИНДЕКСЫ
// ========================================

ContentVersionSchema.index({ originalId: 1, 'version.full': -1 });
ContentVersionSchema.index({ moduleCode: 1 });
ContentVersionSchema.index({ 'hierarchy.path': 1 });
ContentVersionSchema.index({ 'detailLevel.code': 1 });
ContentVersionSchema.index({ 'status.isCurrent': 1 });
```

### Пример Документа

```javascript
{
  _id: ObjectId("..."),
  originalId: ObjectId("original-item-id"),

  // ВЕРСИОНИРОВАНИЕ
  version: {
    major: 2,
    minor: 3,
    patch: 0,
    full: "v2.3.0"
  },

  moduleCode: "MOD-MENU-001",

  hierarchy: {
    path: "1.2.3",
    level: 3,
    parentPath: "1.2"
  },

  detailLevel: {
    code: "L3",
    characterCount: 487,
    sentenceCount: 8,
    wordCount: 72
  },

  revision: {
    code: "REV-2024-12-28-003",
    number: 3,
    timestamp: ISODate("2024-12-28T15:30:00Z")
  },

  // КОНТЕНТ
  content: {
    mainTitle: "Меню Ресторана",

    headers: [
      {
        level: 1,
        text: "Наше меню включает супы и вторые блюда.",
        detailLevel: "L1"
      },
      {
        level: 2,
        text: "Мы предлагаем большой выбор супов: борщ, солянка, уха...",
        detailLevel: "L2"
      }
    ],

    description: "Ресторан \"Ла Петит\" гордится своей аутентичной французской кухней. Каждое блюдо готовится нашим шеф-поваром Жаном-Пьером Дюпоном, имеющим 20-летний опыт работы в Париже. Супы варятся на натуральных бульонах из фермерских продуктов. Борщ готовится по традиционному украинскому рецепту с говядиной и свеклой. Солянка включает три вида мяса и пикантные соленые огурцы. Уха готовится из свежей речной рыбы, которая поставляется ежедневно...",

    descriptionHistory: [
      {
        text: "Наше меню включает супы и вторые блюда.",
        detailLevel: "L1",
        timestamp: ISODate("2024-01-15T10:00:00Z"),
        becameHeaderAtVersion: "v2.0.0"
      },
      {
        text: "Мы предлагаем большой выбор супов: борщ, солянка, уха. Вторые блюда представлены стейками, пастой, рыбными деликатесами.",
        detailLevel: "L2",
        timestamp: ISODate("2024-06-10T14:00:00Z"),
        becameHeaderAtVersion: "v2.3.0"
      }
    ]
  },

  // СВЯЗИ
  previousVersion: ObjectId("v2.2.0-id"),
  nextVersion: null,  // Текущая версия
  parentId: ObjectId("menu-root-id"),
  children: [ObjectId("soup-1"), ObjectId("soup-2")],

  // ТРАНСФОРМАЦИИ
  transformations: [
    {
      type: "expansion",
      fromLevel: "L2",
      toLevel: "L3",
      timestamp: ISODate("2024-12-28T15:30:00Z"),
      triggeredBy: "admin_update",
      automatedBy: "content_expansion_rule"
    },
    {
      type: "promotion_to_header",
      fromLevel: "L2",
      toLevel: "header_L2",
      timestamp: ISODate("2024-12-28T15:30:00Z"),
      triggeredBy: "new_detailed_content",
      automatedBy: "auto_promotion_rule"
    }
  ],

  status: {
    isCurrent: true,
    isArchived: false,
    isPromoted: false
  },

  timestamps: {
    createdAt: ISODate("2024-12-28T15:30:00Z"),
    updatedAt: ISODate("2024-12-28T15:30:00Z"),
    promotedAt: null,
    archivedAt: null
  }
}
```

---

## ⚙️ Алгоритм Автоматического Преобразования

### Правило 1: Расширение Контента

```javascript
async function handleContentExpansion(itemId, newContent) {
  const currentItem = await ContentVersion.findOne({
    originalId: itemId,
    'status.isCurrent': true
  });

  const newWordCount = countWords(newContent);
  const currentWordCount = currentItem.detailLevel.wordCount;

  // Если новый контент существенно длиннее (>50%)
  if (newWordCount > currentWordCount * 1.5) {

    // 1. Создать новую версию
    const newVersion = await createNewVersion(currentItem, {
      description: newContent,
      detailLevel: determineDetailLevel(newWordCount)
    });

    // 2. Текущее описание становится заголовком
    await promoteToHeader(currentItem, newVersion);

    // 3. Обновить связи
    currentItem.nextVersion = newVersion._id;
    currentItem.status.isCurrent = false;
    await currentItem.save();

    newVersion.previousVersion = currentItem._id;
    newVersion.status.isCurrent = true;
    await newVersion.save();

    return newVersion;
  }
}
```

### Правило 2: Продвижение в Заголовки

```javascript
async function promoteToHeader(oldVersion, newVersion) {
  // Текущее описание добавляется в заголовки новой версии
  newVersion.content.headers.push({
    level: newVersion.content.headers.length + 1,
    text: oldVersion.content.description,
    detailLevel: oldVersion.detailLevel.code
  });

  // Добавить в историю
  newVersion.content.descriptionHistory.push({
    text: oldVersion.content.description,
    detailLevel: oldVersion.detailLevel.code,
    timestamp: new Date(),
    becameHeaderAtVersion: newVersion.version.full
  });

  // Записать трансформацию
  newVersion.transformations.push({
    type: 'promotion_to_header',
    fromLevel: oldVersion.detailLevel.code,
    toLevel: `header_${oldVersion.detailLevel.code}`,
    timestamp: new Date(),
    triggeredBy: 'new_detailed_content',
    automatedBy: 'auto_promotion_rule'
  });

  await newVersion.save();
}
```

### Правило 3: Определение Уровня Детализации

```javascript
function determineDetailLevel(wordCount) {
  if (wordCount <= 10) return 'L0';       // Заголовок
  if (wordCount <= 30) return 'L1';       // Краткий
  if (wordCount <= 100) return 'L2';      // Средний
  if (wordCount <= 300) return 'L3';      // Расширенный
  if (wordCount <= 1000) return 'L4';     // Детальный
  if (wordCount <= 3000) return 'L5';     // Полный
  return 'L6+';                           // Исчерпывающий
}
```

---

## 🔌 Система Якорей и Плагинов

### Концепция

**Якорь** - это точка подключения внешнего контента к главному каталогу.

```
Главный Каталог (Статический Скелет)
         ↓
    [Якорь-001]  ← Виджет меню (PHP)
         ↓
    [Якорь-002]  ← Галерея (Python)
         ↓
    [Якорь-003]  ← Каталог товаров (Node.js)
         ↓
    [Якорь-004]  ← Внешний API (любой язык)
```

### Схема Якоря

```javascript
const AnchorSchema = new mongoose.Schema({
  // Уникальный код якоря
  anchorCode: {
    type: String,
    required: true,
    unique: true
    // Формат: "ANCHOR-{TYPE}-{NUMBER}"
  },

  // Название
  name: String,

  // Тип якоря
  type: {
    type: String,
    enum: ['widget', 'page', 'api', 'iframe', 'component'],
    required: true
  },

  // Источник контента
  source: {
    url: String,              // URL внешнего ресурса
    endpoint: String,         // API endpoint
    method: String,           // GET, POST, etc.
    format: String,           // json, xml, html
    language: String,         // php, python, nodejs, etc.
    refreshInterval: Number   // Интервал обновления (мс)
  },

  // Позиция в каталоге
  position: {
    parentId: ObjectId,       // Родитель в каталоге
    order: Number,
    hierarchyPath: String
  },

  // Конфигурация
  config: {
    autoUpdate: Boolean,      // Автоматическое обновление
    cache: Boolean,           // Кэшировать результат
    cacheTTL: Number,         // Время жизни кэша
    transform: String         // Функция преобразования данных
  },

  // Последнее обновление
  lastUpdate: {
    timestamp: Date,
    status: String,           // success, error
    data: mongoose.Schema.Types.Mixed,
    error: String
  },

  // Метаданные
  metadata: {
    version: String,
    author: String,
    tags: [String]
  }
});
```

### API для Подключения Якорей

```javascript
// POST /api/anchors - Создать якорь
POST /api/anchors
{
  "name": "Меню Ресторана",
  "type": "widget",
  "source": {
    "url": "https://restaurant.com/menu.php",
    "format": "json",
    "language": "php",
    "refreshInterval": 3600000  // 1 час
  },
  "position": {
    "parentId": "main-menu-id",
    "order": 1
  },
  "config": {
    "autoUpdate": true,
    "cache": true,
    "cacheTTL": 1800000  // 30 минут
  }
}

// GET /api/anchors/:code - Получить данные якоря
GET /api/anchors/ANCHOR-WIDGET-001

// PUT /api/anchors/:code/refresh - Обновить данные
PUT /api/anchors/ANCHOR-WIDGET-001/refresh
```

### Примеры Якорей

#### 1. PHP Виджет Меню

```javascript
{
  anchorCode: "ANCHOR-MENU-001",
  type: "widget",
  source: {
    url: "https://restaurant.com/api/menu.php",
    format: "json",
    language: "php"
  }
}
```

#### 2. Python Галерея

```javascript
{
  anchorCode: "ANCHOR-GALLERY-001",
  type: "component",
  source: {
    url: "https://photos.com/api/v1/gallery",
    format: "json",
    language: "python"
  }
}
```

#### 3. Внешний Iframe

```javascript
{
  anchorCode: "ANCHOR-IFRAME-001",
  type: "iframe",
  source: {
    url: "https://external-service.com/widget",
    format: "html"
  }
}
```

---

## 📈 Визуализация Версионирования

### Временная Линия Версий

```
v1.0 ──→ v1.1 ──→ v1.2 ──→ v2.0 ──→ v2.1 ──→ v3.0
 │        │        │        │        │        │
L1       L2       L2       L3       L3       L4
(2 пр.)  (6 пр.)  (6 пр.)  (20 пр.) (20 пр.) (50 пр.)
```

### Дерево Иерархии + Версии

```
Меню (v3.0, L0)
├─ 1.1 Супы (v2.5, L2)
│  ├─ 1.1.1 Борщ (v2.0, L3)
│  │  ├─ 1.1.1.1 История борща (v1.0, L4)
│  │  └─ 1.1.1.2 Рецепт (v3.2, L5)
│  └─ 1.1.2 Солянка (v1.8, L3)
└─ 1.2 Вторые блюда (v3.1, L2)
   └─ 1.2.1 Стейки (v2.3, L4)
```

---

## 🎓 Примеры Использования

### Пример 1: Эволюция Статьи в Энциклопедии

**Начальное состояние (v1.0):**
```
Заголовок: "Париж"
Описание (L1): "Париж - столица Франции."
```

**После расширения (v2.0):**
```
Заголовок: "Париж"
Под-заголовок-1: "Париж - столица Франции."
Описание (L2): "Париж - столица и крупнейший город Франции.
                Население около 2.2 млн человек. Основан в 3 веке.
                Известен Эйфелевой башней, Лувром, Нотр-Дамом."
```

**После дальнейшего расширения (v3.0):**
```
Заголовок: "Париж"
Под-заголовок-1: "Париж - столица Франции."
Под-заголовок-2: "Париж - столица и крупнейший город Франции..."
Описание (L3): [Детальное описание истории, географии,
                культуры, достопримечательностей - 500 слов]
```

### Пример 2: Меню Ресторана с Обновлениями

**Версия 1.0 (Январь):**
```
Меню (L1): "Супы, вторые блюда, десерты."
```

**Версия 2.0 (Март - добавили описания):**
```
Меню
  └─ Заголовок: "Супы, вторые блюда, десерты."
  └─ Описание (L2): "В нашем меню 5 видов супов, 12 вторых блюд,
                     8 десертов. Все блюда готовятся из свежих
                     продуктов."
```

**Версия 3.0 (Июнь - добавили детали каждого блюда):**
```
Меню
  └─ Заголовок-1: "Супы, вторые блюда, десерты."
  └─ Заголовок-2: "В нашем меню 5 видов супов..."
  └─ Описание (L3): [Подробное описание каждого блюда
                     с ценами, составом, калориями]
  └─ Дочерние элементы:
      ├─ 1.1 Супы (L2)
      ├─ 1.2 Вторые блюда (L3)
      └─ 1.3 Десерты (L2)
```

---

## 🚀 Следующие Шаги

1. ✅ Концепция версионирования (этот документ)
2. 📋 Схема базы данных
3. 💻 API для версионирования
4. 🔌 Система якорей
5. 🎨 Обновленный прототип
6. 🧪 Тестирование

---

**Версия:** 1.0
**Дата:** 2024-12-28
**Статус:** Концепция Многомерного Версионирования
