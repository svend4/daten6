# Приложение 2: Интеграция со Свободной Энциклопедией и Автоматическая Компиляция

## 📋 Статус Документа

**Тип:** Приложение к основной документации
**Версия:** 1.0
**Дата:** 2024-12-28
**Связанные документы:**
- [APPENDIX_1_AFFILIATE_FEEDBACK.md](APPENDIX_1_AFFILIATE_FEEDBACK.md)
- [VERSIONING_SYSTEM.md](VERSIONING_SYSTEM.md)

---

## 🎯 Концепция: Свободная Энциклопедия vs Википедия

### Сравнение Подходов

| Критерий | Википедия | Свободная Энциклопедия Туризма |
|----------|-----------|--------------------------------|
| **Модерация** | Жёсткая | Свободная (с базовой проверкой) |
| **Источники** | Строгие требования | Отзывы туристов |
| **Стиль** | Энциклопедический | Живой, эмоциональный |
| **Обновления** | Ручные правки | Автоматическая компиляция |
| **Мнения** | Нейтральные | Личный опыт приветствуется |
| **Цитаты** | Из научных источников | Из отзывов путешественников |
| **Верификация** | Требуется | Опциональная |

### Модель Свободной Энциклопедии

```
Свободная Энциклопедия = Wikipedia + TripAdvisor + Личный Опыт

Википедия:
- Структура статей
- Категории
- Ссылки между статьями

TripAdvisor:
- Отзывы туристов
- Рейтинги
- Фотографии

Личный Опыт:
- Эмоции
- Личные истории
- Практические советы
```

---

## 📚 Структура Статьи Свободной Энциклопедии

### Шаблон Статьи

```javascript
const WikiArticleSchema = new mongoose.Schema({
  // Основная информация
  articleId: String,
  title: String,
  slug: String,

  // Категория
  category: {
    type: String,
    enum: ['city', 'country', 'attraction', 'hotel', 'restaurant', 'experience'],
    index: true
  },

  // Структура контента
  content: {
    // 1. Энциклопедическая часть (как в Wikipedia)
    encyclopedia: {
      introduction: String,        // Краткое введение
      history: String,             // История
      geography: String,           // География
      culture: String,             // Культура
      sources: [String]            // Источники (Wikipedia, книги)
    },

    // 2. Живые отзывы (уникальная часть)
    liveContent: {
      // Секции с цитатами
      sections: [{
        title: String,             // "Что посмотреть"
        category: String,          // attractions
        intro: String,             // Вступление
        quotes: [{
          text: String,            // Текст цитаты
          author: String,          // Автор
          source: String,          // Источник (партнёр)
          rating: Number,          // Рейтинг (если есть)
          date: Date               // Дата
        }],
        conclusion: String         // Заключение секции
      }]
    },

    // 3. Практическая информация
    practical: {
      howToGet: String,            // Как добраться
      bestTime: String,            // Лучшее время для посещения
      budget: String,              // Бюджет
      tips: [String]               // Советы
    },

    // 4. Мультимедиа
    media: {
      mainImage: String,
      gallery: [String],
      videos: [String]
    }
  },

  // Метаданные
  metadata: {
    location: {
      country: String,
      city: String,
      coordinates: {
        lat: Number,
        lng: Number
      }
    },
    tags: [String],
    relatedArticles: [String]
  },

  // Статистика компиляции
  compilation: {
    totalQuotes: Number,
    totalSources: Number,
    partners: [{
      partnerId: String,
      quotesContributed: Number
    }],
    lastCompiled: Date,
    autoGenerated: Boolean       // Автогенерация vs ручная правка
  },

  // Версионирование (из VERSIONING_SYSTEM)
  version: {
    major: Number,
    minor: Number,
    full: String
  },

  // Статистика просмотров
  stats: {
    views: Number,
    likes: Number,
    shares: Number,
    comments: Number
  }
});
```

---

## 🤖 Автоматическая Компиляция с AI/ML (Опционально)

### Уровни Автоматизации

#### Уровень 1: Простой (Без AI)

**Используется:**
- Регулярные выражения
- Ключевые слова
- Шаблоны

**Пример из APPENDIX_1:**
```javascript
// Экстракция по кавычкам
const quotedPattern = /"([^"]+)"/g;

// Оценка важности по ключевым словам
const keywords = ['потрясающий', 'незабываемый'];
```

**Плюсы:**
- ✅ Быстро
- ✅ Предсказуемо
- ✅ Не требует ML

**Минусы:**
- ❌ Ограниченная точность
- ❌ Пропускает сложные случаи

#### Уровень 2: С AI/ML (Рекомендуется для больших объёмов)

**Используется:**
- NLP (Natural Language Processing)
- Sentiment Analysis
- Named Entity Recognition
- Summarization

**Технологии:**
- OpenAI GPT API (простая интеграция)
- spaCy (Python, self-hosted)
- Transformers (Hugging Face)

**Пример с OpenAI:**

```javascript
const OpenAI = require('openai');
const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

class AIQuoteExtractor {
  /**
   * Извлечение цитат с помощью GPT
   */
  async extractQuotesWithAI(reviewText) {
    const prompt = `
Извлеки из следующего отзыва туриста наиболее важные и эмоциональные
цитаты, которые можно использовать в энциклопедической статье.

Отзыв:
"${reviewText}"

Верни результат в JSON формате:
{
  "quotes": [
    {
      "text": "текст цитаты",
      "category": "location|food|service|experience",
      "importance": 0-100,
      "emotion": "positive|negative|neutral"
    }
  ]
}
    `;

    const response = await openai.chat.completions.create({
      model: "gpt-4",
      messages: [{ role: "user", content: prompt }],
      temperature: 0.3,
      response_format: { type: "json_object" }
    });

    return JSON.parse(response.choices[0].message.content);
  }

  /**
   * Компиляция секции статьи с помощью AI
   */
  async compileSectionWithAI(quotes, category) {
    const quotesText = quotes.map((q, i) =>
      `${i+1}. "${q.text}" — ${q.author}`
    ).join('\n');

    const prompt = `
Ты составляешь статью для свободной энциклопедии туризма.
Напиши небольшую секцию (2-3 абзаца) на тему "${category}",
используя следующие цитаты реальных туристов.

Стиль: живой, но информативный.
Формат: энциклопедическая статья с вкраплениями цитат.

Цитаты:
${quotesText}

Используй цитаты в тексте в формате:
> "текст цитаты"
> — Автор
    `;

    const response = await openai.chat.completions.create({
      model: "gpt-4",
      messages: [{ role: "user", content: prompt }],
      temperature: 0.7
    });

    return response.choices[0].message.content;
  }

  /**
   * Суммаризация множества отзывов
   */
  async summarizeReviews(reviews) {
    const reviewsText = reviews.map(r => r.content.text).join('\n\n---\n\n');

    const prompt = `
Прочитай следующие отзывы туристов и составь краткое резюме
(3-4 предложения), отражающее общее мнение.

Отзывы:
${reviewsText}
    `;

    const response = await openai.chat.completions.create({
      model: "gpt-4",
      messages: [{ role: "user", content: prompt }],
      temperature: 0.5,
      max_tokens: 200
    });

    return response.choices[0].message.content;
  }
}
```

**Пример с spaCy (Python - self-hosted):**

```python
import spacy
from spacy.lang.ru import Russian

class SpacyQuoteExtractor:
    def __init__(self):
        self.nlp = spacy.load("ru_core_news_lg")

    def extract_entities(self, text):
        """Извлечение именованных сущностей"""
        doc = self.nlp(text)

        entities = {
            'locations': [],
            'persons': [],
            'organizations': []
        }

        for ent in doc.ents:
            if ent.label_ == 'LOC':
                entities['locations'].append(ent.text)
            elif ent.label_ == 'PER':
                entities['persons'].append(ent.text)
            elif ent.label_ == 'ORG':
                entities['organizations'].append(ent.text)

        return entities

    def analyze_sentiment(self, text):
        """Анализ тональности"""
        # Простой подход на основе словаря
        positive_words = ['отлично', 'прекрасно', 'восхитительно']
        negative_words = ['плохо', 'ужасно', 'отвратительно']

        text_lower = text.lower()

        pos_count = sum(1 for word in positive_words if word in text_lower)
        neg_count = sum(1 for word in negative_words if word in text_lower)

        if pos_count > neg_count:
            return 'positive'
        elif neg_count > pos_count:
            return 'negative'
        else:
            return 'neutral'

    def extract_key_phrases(self, text):
        """Извлечение ключевых фраз"""
        doc = self.nlp(text)

        # Существительные + прилагательные
        key_phrases = []
        for chunk in doc.noun_chunks:
            if len(chunk.text.split()) >= 2:
                key_phrases.append(chunk.text)

        return key_phrases
```

---

## 🔄 Workflow Автоматической Компиляции

### Полный Процесс

```
1. ИМПОРТ ОТЗЫВОВ
   ├─ RSS/Atom feed
   ├─ API партнёров
   └─ Ручной ввод
          ↓

2. ЭКСТРАКЦИЯ ЦИТАТ
   ├─ Простой метод (regex, keywords)
   └─ AI метод (GPT, spaCy)
          ↓

3. КАТАЛОГ ЦИТАТ
   ├─ Сохранение
   ├─ Категоризация
   ├─ Проверка уникальности
   └─ Оценка важности
          ↓

4. ГРУППИРОВКА
   ├─ По местам (Венеция, Париж)
   ├─ По категориям (еда, сервис)
   └─ По темам (романтика, семья)
          ↓

5. КОМПИЛЯЦИЯ СТАТЬИ
   ├─ Шаблонная (простой метод)
   └─ AI-генерация (GPT-4)
          ↓

6. МОДЕРАЦИЯ
   ├─ Автоматическая проверка
   ├─ Ручная модерация (опционально)
   └─ Утверждение
          ↓

7. ПУБЛИКАЦИЯ
   ├─ В энциклопедии
   ├─ Виджеты партнёрам
   └─ Уведомления авторов цитат
          ↓

8. ОБНОВЛЕНИЕ
   ├─ Новые отзывы → новые цитаты
   ├─ Автообновление статьи
   └─ Версионирование
```

---

## 🌐 Интеграция с Wikipedia

### Гибридный Подход

**Идея:** Комбинировать данные из Wikipedia с живыми отзывами

```javascript
class WikipediaIntegration {
  /**
   * Получение базовой информации из Wikipedia
   */
  async fetchWikipediaData(topic) {
    const axios = require('axios');

    const response = await axios.get(
      `https://ru.wikipedia.org/api/rest_v1/page/summary/${encodeURIComponent(topic)}`
    );

    return {
      title: response.data.title,
      introduction: response.data.extract,
      thumbnail: response.data.thumbnail?.source,
      url: response.data.content_urls.desktop.page
    };
  }

  /**
   * Создание гибридной статьи
   */
  async createHybridArticle(topic, quotes) {
    // 1. Получить данные из Wikipedia
    const wikiData = await this.fetchWikipediaData(topic);

    // 2. Скомпилировать отзывы
    const compiler = new ArticleCompiler();
    const compiledReviews = await compiler.compileArticle(topic, quotes);

    // 3. Объединить
    const hybridArticle = {
      title: wikiData.title,

      content: {
        // Энциклопедическая часть (из Wikipedia)
        encyclopedia: {
          introduction: wikiData.introduction,
          source: 'Wikipedia',
          sourceUrl: wikiData.url
        },

        // Живые отзывы (скомпилированные)
        liveContent: compiledReviews.sections,

        // Практическая информация (комбинация)
        practical: {
          // Можно добавить из отзывов
        }
      },

      metadata: {
        wikiImage: wikiData.thumbnail,
        compilation: {
          totalQuotes: quotes.length,
          autoGenerated: true
        }
      }
    };

    return hybridArticle;
  }
}
```

---

## 📝 Форматы Вывода

### 1. Markdown (для Энциклопедии)

```markdown
# Венеция: Город на Воде

*Из Wikipedia:* Венеция — город в Италии, расположенный на 118 островах...

## Что Посмотреть

Туристы особенно рекомендуют следующие достопримечательности:

> "Площадь Сан-Марко абсолютно потрясающая! Вечером здесь играет живая музыка."
> — Мария Иванова, Россия

Другие путешественники отмечают:

> "Обязательно прокатитесь на гондоле! Это незабываемый опыт."
> — John Smith, USA

## Где Поесть

Кулинарные впечатления гостей города:

> "Ristorante Da Ivo — лучшая паста с морепродуктами, что я пробовал!"
> — Pierre Dupont, France

## Практические Советы

**Как добраться:** Аэропорт Marco Polo, затем водное такси.

**Лучшее время:** Апрель-июнь, Сентябрь-Октябрь (избегайте июля-августа).

**Бюджет:** €100-200 в день (средний уровень).

---

*Статья составлена на основе 225 отзывов от 3 партнёров.*
*Последнее обновление: 28 декабря 2024*
```

### 2. HTML (для Веба)

```html
<article class="wiki-article">
  <header>
    <h1>Венеция: Город на Воде</h1>
    <div class="wiki-source">
      <small>Основано на <a href="https://ru.wikipedia.org">Wikipedia</a>
      и 225 отзывах туристов</small>
    </div>
  </header>

  <section class="encyclopedia-section">
    <h2>Введение</h2>
    <p>Венеция — город в Италии, расположенный на 118 островах...</p>
    <cite>Источник: Wikipedia</cite>
  </section>

  <section class="live-content-section">
    <h2>Что Посмотреть</h2>
    <p>Туристы особенно рекомендуют следующие достопримечательности:</p>

    <blockquote class="tourist-quote">
      <p>"Площадь Сан-Марко абсолютно потрясающая!
         Вечером здесь играет живая музыка."</p>
      <footer>
        — Мария Иванова,
        <cite>Grand Hotel Venezia</cite>
      </footer>
    </blockquote>

    <!-- Больше цитат -->
  </section>

  <aside class="practical-info">
    <h3>Практическая Информация</h3>
    <dl>
      <dt>Как добраться</dt>
      <dd>Аэропорт Marco Polo, затем водное такси</dd>

      <dt>Лучшее время</dt>
      <dd>Апрель-Июнь, Сентябрь-Октябрь</dd>

      <dt>Бюджет</dt>
      <dd>€100-200 в день</dd>
    </dl>
  </aside>

  <footer class="article-meta">
    <p>Составлено на основе 225 отзывов от 3 партнёров</p>
    <time datetime="2024-12-28">Обновлено: 28 декабря 2024</time>
  </footer>
</article>
```

### 3. JSON (для API)

```json
{
  "articleId": "ART-VENICE-001",
  "title": "Венеция: Город на Воде",
  "version": "v2.5.0",

  "content": {
    "encyclopedia": {
      "introduction": "Венеция — город в Италии...",
      "source": "Wikipedia",
      "sourceUrl": "https://ru.wikipedia.org/wiki/Венеция"
    },

    "liveContent": {
      "sections": [
        {
          "title": "Что Посмотреть",
          "category": "attractions",
          "quotes": [
            {
              "text": "Площадь Сан-Марко абсолютно потрясающая!",
              "author": "Мария Иванова",
              "source": "Grand Hotel Venezia",
              "importance": 95
            }
          ]
        }
      ]
    }
  },

  "metadata": {
    "location": {
      "country": "Италия",
      "city": "Венеция",
      "coordinates": { "lat": 45.4408, "lng": 12.3155 }
    },
    "tags": ["италия", "венеция", "путешествия", "романтика"],
    "relatedArticles": ["ART-ITALY-001", "ART-FLORENCE-001"]
  },

  "compilation": {
    "totalQuotes": 156,
    "totalSources": 225,
    "partners": [
      {
        "partnerId": "PARTNER-HOTEL-001",
        "name": "Grand Hotel Venezia",
        "quotesContributed": 67
      }
    ],
    "lastCompiled": "2024-12-28T10:00:00Z",
    "autoGenerated": true
  },

  "stats": {
    "views": 15234,
    "likes": 456,
    "shares": 123
  }
}
```

---

## 🔔 Уведомления и Обратная Связь

### Уведомления Авторам Цитат

```javascript
class NotificationService {
  /**
   * Уведомление автору отзыва
   */
  async notifyQuoteAuthor(quote, article) {
    const email = {
      to: quote.author.email,
      subject: 'Ваш отзыв использован в статье энциклопедии!',
      template: 'quote-used-notification',
      data: {
        authorName: quote.author.name,
        quoteText: quote.text,
        articleTitle: article.title,
        articleUrl: `https://travel-wiki.com/articles/${article.slug}`,
        partnerName: quote.source.partnerName
      }
    };

    await this.sendEmail(email);
  }

  /**
   * Уведомление партнёру
   */
  async notifyPartner(partner, article, quotesCount) {
    const email = {
      to: partner.contact.email,
      subject: 'Новая статья с вашими отзывами опубликована!',
      template: 'partner-article-notification',
      data: {
        partnerName: partner.name,
        articleTitle: article.title,
        quotesUsed: quotesCount,
        articleUrl: `https://travel-wiki.com/articles/${article.slug}`,
        widgetCode: await this.generateWidgetCode(partner, article)
      }
    };

    await this.sendEmail(email);
  }
}
```

---

## 🎯 Use Case: Полный Цикл

### Сценарий: Создание Статьи "Венеция"

**День 1: Импорт отзывов**
```
10:00 - Автоматическая синхронизация с 3 партнёрами
- Grand Hotel Venezia: 20 новых отзывов
- Ristorante Italiano: 15 новых отзывов
- Venice Tours: 10 новых отзывов
Итого: 45 новых отзывов
```

**День 1: Обработка (10:30)**
```javascript
// Автоматическая экстракция цитат
for (const review of newReviews) {
  const quotes = await extractor.extractQuotesWithAI(review.content.text);

  for (const quote of quotes) {
    if (quote.importance >= 70 && await isUnique(quote)) {
      await QuoteCatalog.create({
        ...quote,
        source: {
          reviewId: review.reviewId,
          partnerId: review.source.partnerId
        }
      });
    }
  }
}

// Результат: Извлечено 87 цитат, из них 34 уникальных
```

**День 1: Компиляция (11:00)**
```javascript
// Автоматическая компиляция статьи
const topic = {
  title: 'Венеция: Город на Воде',
  locations: ['Венеция', 'Италия'],
  keywords: ['венеция', 'гондола', 'сан-марко']
};

const article = await compileHybridArticle(topic);

// Результат: Создана статья v1.0 с 34 цитатами
```

**День 1: Модерация (11:30)**
```
Автоматическая проверка:
✅ Нет спама
✅ Нет оскорблений
✅ Цитаты релевантны

Статус: Одобрена для публикации
```

**День 1: Публикация (12:00)**
```
✅ Статья опубликована в энциклопедии
✅ Уведомления отправлены 34 авторам цитат
✅ Уведомления отправлены 3 партнёрам
✅ Виджеты сгенерированы для партнёров
```

**День 7: Обновление**
```
Новых отзывов: 120
Новых цитат: 45 (из них 18 уникальных)

Автоматическое обновление статьи:
v1.0 → v1.1
- Добавлено 18 новых цитат
- Обновлены секции "Что посмотреть" и "Где поесть"
- Добавлена новая секция "Советы для семей с детьми"
```

**Через месяц:**
```
Статья: "Венеция: Город на Воде" v2.3
- Всего отзывов обработано: 847
- Цитат извлечено: 234
- Цитат в статье: 89 (самые важные и уникальные)
- Партнёров-источников: 12
- Просмотров: 25,431
- Лайков: 1,234
```

---

## 📊 Метрики Качества

### KPI Системы

```javascript
{
  // Охват
  coverage: {
    totalReviews: 2500,
    processedReviews: 2450,
    processingRate: 98        // %
  },

  // Экстракция
  extraction: {
    totalQuotes: 5670,
    uniqueQuotes: 1234,
    uniquenessRate: 21.8      // %
  },

  // Компиляция
  compilation: {
    totalArticles: 45,
    autoGenerated: 42,
    manuallyEdited: 3,
    avgQuotesPerArticle: 27
  },

  // Качество
  quality: {
    avgImportanceScore: 78,   // 0-100
    moderationApprovalRate: 94, // %
    userSatisfaction: 4.5     // 1-5
  },

  // Влияние
  impact: {
    totalViews: 125000,
    avgTimeOnPage: 245,       // секунды
    bounceRate: 35            // %
  }
}
```

---

## 🔐 Этика и Прозрачность

### Принципы

1. **Прозрачность Источников**
   - Всегда указывать автора цитаты
   - Ссылка на оригинальный отзыв
   - Указание партнёра-источника

2. **Согласие Авторов**
   - Опциональное требование согласия
   - Возможность отозвать цитату
   - Право на анонимность

3. **Качество Контента**
   - Модерация на спам
   - Проверка релевантности
   - Баланс мнений (не только позитивные)

4. **Честность с Партнёрами**
   - Прозрачная статистика
   - Честное начисление комиссий
   - Обратная связь

---

## 🚀 Roadmap Реализации

### Фаза 1: MVP (4 недели)
- [ ] Базовая экстракция цитат (regex)
- [ ] Простой компилятор статей (шаблоны)
- [ ] API для партнёров
- [ ] Базовая модерация

### Фаза 2: Автоматизация (6 недель)
- [ ] AI экстракция (GPT/spaCy)
- [ ] AI компиляция статей
- [ ] RSS/Atom интеграция
- [ ] Автоматические уведомления

### Фаза 3: Энциклопедия (4 недели)
- [ ] Интеграция с Wikipedia
- [ ] Публичный интерфейс энциклопедии
- [ ] Виджеты для партнёров
- [ ] Аналитика и метрики

### Фаза 4: Оптимизация (2 недели)
- [ ] Улучшение алгоритмов
- [ ] A/B тестирование
- [ ] SEO оптимизация
- [ ] Performance tuning

---

**Версия:** 1.0
**Дата:** 2024-12-28
**Статус:** Приложение - Интеграция со Свободной Энциклопедией
